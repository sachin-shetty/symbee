<project name="buildserver" default="dist" basedir="C:\sachin\work\shoonya\google_symbee">

    <path id="javac.classpath">
        <pathelement path="${basedir}/${build}"/>
        <pathelement path="${basedir}/src/java"/>
        <pathelement path="C:\Program Files\Java\jdk1.6.0_01\jre\lib\javaws.jar"/>
        <fileset dir="${basedir}/lib">
           <include name="*"/>
       </fileset>
    </path>

    <property name="src" location="src"/>
    <property name="build" location="build"/>
    <property name="dist"  location="dist"/>

    <target name="init" depends="clean">
        <!-- Create the time stamp -->
        <tstamp/>
        <!-- Create the build directory structure used by compile -->
        <mkdir dir="${build}"/>
        <mkdir dir="${dist}"/>
    </target>

    <target name="compile" depends="init"
            description="compile the source " >
        <!-- Compile the java code from ${src} into ${build} -->
        <javac
                target="1.6"
                verbose="yes"
                srcdir="${src}/java"
                destdir="${build}"
                debug="true"
                classpathref="javac.classpath"
                />
    </target>

    <target name="dist" depends="compile"
            description="generate the server distribution" >
        <!-- Create the distribution directory -->
        <copydir src="${basedir}/src"  dest="${build}/src"/>
        <!-- Put everything in ${build} into the MyProject-${DSTAMP}.jar file -->
        <jar jarfile="${dist}/symbee_server.jar" basedir="${build}"/>
        <delete dir="${build}" />
    </target>

    <target name="clean"
            description="clean up" >
        <!-- Delete the ${build} and ${dist} directory trees -->
        <delete dir="${build}" failonerror="false" />
        <delete dir="${dist}" failonerror="false"/>
    </target>

</project>